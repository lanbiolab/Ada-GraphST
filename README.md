# Ada-GraphST
ries between adjacent but distinct tissue regions. Here, we present Ada-GraphST, a boundary-aware spatial domain analysis method that replaces the static neighborhood graph with an adaptive graph guided by molecular similarity, enabling more precise delineation of tissue boundaries while preserving spatial coherence.
